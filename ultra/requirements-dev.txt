# ULTRA Algorithmic Trading Platform - Development Dependencies
# Install: pip install -r requirements-dev.txt
-r requirements.txt

# Test / CI (pinned to the versions that pass on the 3.11/3.12 matrix)
pytest==9.1.1
pytest-asyncio==1.4.0

# Static validation (deploy YAML checks in scripts/criteria.py)
PyYAML==6.0.3
