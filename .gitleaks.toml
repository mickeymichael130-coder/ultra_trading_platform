title = "ULTRA Algorithmic Trading Platform gitleaks config"

# Use the full default rule set. No allowlist entries: the repository
# history is clean (no venv/__pycache__/logs/data ever committed), so any
# finding is a real secret and should fail CI.
[extend]
useDefault = true
